import 'package:flutter/material.dart';

const String packageName = 'cherry_toast';

const TextStyle defaultTitleStyle = TextStyle(
  color: Colors.black,
  fontWeight: FontWeight.bold,
);
const TextStyle defaultDescriptionStyle = TextStyle(
  color: Colors.black,
);

const Duration defaultAnimationDuration = Duration(
  milliseconds: 1500,
);
const Duration defaultToastDuration = Duration(
  milliseconds: 3000,
);
const Cubic defaultAnimationCurve = Curves.ease;

const double closeButtonSize = 15;

const double defaultIconSize = 20;
const double defaultIconLayoutSize = 40;
const double defaultRadius = 20;
