enum CHERRY_TYPE {
  success,
  warning,
  error,
  info,
  custom,
}

enum POSITION {
  top,
  bottom,
}

enum ANIMATION_TYPE {
  fromTop,
  fromLeft,
  fromRight,
}

enum TOAST_LAYOUT {
  ltr,
  rtl,
}
