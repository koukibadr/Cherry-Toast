enum CherryType {
  success,
  warning,
  error,
  info,
  custom,
}

enum Position {
  top,
  bottom,
}

enum AnimationType {
  fromTop,
  fromLeft,
  fromRight,
}

enum ToastLayout {
  ltr,
  rtl,
}
